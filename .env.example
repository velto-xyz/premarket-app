# Environment Variables Template
# Copy to .env.local for local development

# =============================================================================
# SUPABASE
# =============================================================================
VITE_SUPABASE_URL="http://127.0.0.1:54321"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-key"

# =============================================================================
# BLOCKCHAIN
# =============================================================================
VITE_RPC_URL="http://127.0.0.1:8545"
VITE_CHAIN_ID="31337"

# WalletConnect Project ID (optional for local dev)
# Get one at https://cloud.walletconnect.com
# VITE_WALLETCONNECT_PROJECT_ID="your-project-id"

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================

# Mapbox (for World Map)
# Get token at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN="your-mapbox-token"

# =============================================================================
# EDGE FUNCTION SECRETS
# =============================================================================
# These are used by Supabase Edge Functions
# For local: set here, edge functions read from process env
# For remote: push with `supabase secrets set KEY=value`

# Envio Indexer (for price history edge function)
INDEXER_URL="http://host.docker.internal:8080/v1/graphql"

# FRED API Key (for economic indicators in sentiment-fetcher)
# Get free key at https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY="your-fred-api-key"

# =============================================================================
# REMOTE DEPLOYMENT
# =============================================================================
# To push secrets to Supabase hosted project:
#
#   supabase secrets set FRED_API_KEY=your-fred-api-key
#
# To list current secrets:
#
#   supabase secrets list
#
